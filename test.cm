/* Sample C-Minus program */
int gcd(int a, int b) {
    if (b == 0) {
        return a;
    } else {
        return gcd(b, a % b);
    }
}

int main() {
    int x = 48;
    int y = 18;
    int result = gcd(x, y);
    return result;
}